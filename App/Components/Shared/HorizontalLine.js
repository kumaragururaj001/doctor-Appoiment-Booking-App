import { View, Text } from "react-native";
import React from "react";

export default function HorizontalLine() {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        borderColor: "#079DAA",
        margin: 5,
        marginBottom: 10,
        marginTop: 20,
      }}
    ></View>
  );
}
